<Window x:Class="PL.ParcelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ParcelWindow" Height="450" Width="800"
        Closing="OnClosing">

    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                    
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid">
        <Grid x:Name="UpgradeGrid" Visibility="Hidden">
            <Button x:Name="UpdateButton" Content="UPDATE" HorizontalAlignment="Left" 
                   
                    Background="Transparent" Margin="648,166,0,0" VerticalAlignment="Top" ToolTip="UPDATE" Height="51" Width="133" BorderBrush="Black" Click="ClickUpdate" Opacity="100" FontFamily="Cambria" FontSize="30" FontWeight="Bold" FontStyle="Italic">
            </Button>
            <!--CloseButton-->
            <Button Name="CloseButton" Content="CLOSE" HorizontalAlignment="Left" Background="Transparent" Margin="472,10,0,0" VerticalAlignment="Top" ToolTip="Close the Window" Height="59" Width="130" BorderBrush="Black" Click="ClickCloseParcelWindow" Opacity="100" FontFamily="Cambria" FontWeight="Bold" FontStyle="Italic" FontSize="30">

            </Button>
        </Grid>
        <Grid x:Name="AddGrid" Visibility="Visible">
            <Button Content="Add" HorizontalAlignment="Left" Height="74" Margin="584,42,0,0" VerticalAlignment="Top" Width="124" Click="Add_Click"/>
            <ComboBox x:Name="Combo_SenderId" HorizontalAlignment="Left" Height="37" SelectedItem="{Binding ClientInParcel.ID, Mode=TwoWay}" Margin="87,68,0,0" VerticalAlignment="Top" Width="232" SelectionChanged="Combo_SenderId_SelectionChanged"/>
            <ComboBox x:Name="Combo_TargetId" HorizontalAlignment="Left" Height="30" SelectedItem="{Binding ClientInParcel.ID,Mode=TwoWay}" Margin="81,170,0,0" VerticalAlignment="Top" Width="229" SelectionChanged="Combo_TargetId_SelectionChanged"/>
            <ComboBox x:Name="Combo_Priority" HorizontalAlignment="Left" Height="34" SelectedItem="{Binding Priority, Mode=TwoWay}" Margin="81,247,0,0" VerticalAlignment="Top" Width="229" SelectionChanged="Combo_Priority_SelectionChanged"/>
            <ComboBox x:Name="Combo_Weight" HorizontalAlignment="Left" Height="31" SelectedItem="{Binding Weight, Mode=TwoWay}" Margin="87,343,0,0" VerticalAlignment="Top" Width="223" SelectionChanged="Combo_Weight_SelectionChanged"/>
            <Label  Content="SenderId" HorizontalAlignment="Left" Height="29" Margin="90,34,0,0" VerticalAlignment="Top" Width="229"/>
            <Label Content="TargetId" HorizontalAlignment="Left" Height="29" Margin="88,139,0,0" VerticalAlignment="Top" Width="230"/>
            <Label Content="Priority" HorizontalAlignment="Left" Height="29" Margin="88,217,0,0" VerticalAlignment="Top" Width="229"/>
            <Label Content="Weight" HorizontalAlignment="Left" Height="29" Margin="89,313,0,0" VerticalAlignment="Top" Width="229"/>
            <Button Content="" HorizontalAlignment="Left" ToolTip="Close" Height="49" Margin="631,353,0,0" VerticalAlignment="Top" Width="120" Click="ClickCloseParcelWindow" FontFamily="Cambria" FontWeight="Bold" Foreground="Red" FontSize="22" FontStyle="Normal" BorderThickness="3,3,3,3" SnapsToDevicePixels="True">
                <Button.Background>
                    <ImageBrush ImageSource="/close.png"/>
                </Button.Background>
            </Button>
            <Button Content="Remove" HorizontalAlignment="Left" Height="64" Margin="501,217,0,0" VerticalAlignment="Top" Width="119" Click="Remove_Button"/>

        </Grid>
    </Grid>
</Window>
